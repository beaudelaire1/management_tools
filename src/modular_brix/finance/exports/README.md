# exports (C15)

Deterministic FEC generation with stored fingerprint and controls.

- A47 A-1 column order; validated entries only; stable sorting; SHA-256 fingerprint stored with the content; sequence-continuity controls record anomalies instead of dropping them.
- Limits: subledger columns (CompAux, lettering dates), multi-entity profiles and reference test datasets remain to implement.
