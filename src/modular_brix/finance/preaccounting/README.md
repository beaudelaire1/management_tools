# preaccounting (C07)

Accountant-ready batches with dated mappings and reproducible content.

- Every exported line keeps its business source; rebuilding a period yields the same content hash; a transmitted batch locks the period (corrections need a new batch); accountant comments carry resolution state; missing source documents are flagged.
- Limits: export profiles, document bundles and cabinet portal access remain to implement.
