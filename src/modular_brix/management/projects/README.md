# projects (G08)

Projects, tasks, acyclic dependencies, milestones, scope-change history.

- `add_dependency` rejects cycles; `blocked_tasks` exposes blocking paths; progress is computed, never stored.
- Limits: phases, budgets, deliverables and client portal access remain to implement.
