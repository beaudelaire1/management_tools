# capacity (P10)

Weekly utilization snapshots computed from scheduling data.

- Bookings are the single workload source (no double counting); absences reduce available capacity; overload is flagged, never absorbed. Steering never writes to domain tables.
